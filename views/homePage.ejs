<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/homePage.css">
    <title>Fitness Tracker</title>
</head>
<body>
<script>
    document.addEventListener('DOMContentLoaded', function () {
        var closeGreetingButton = document.getElementById("closeGreetingButton");
        var mainSection = document.querySelector("section");

        closeGreetingButton.addEventListener("click", function () {
            var greetingDiv = document.getElementById("greetingDiv");
            greetingDiv.remove();

            // Remove the 'blur' class from the main section
            mainSection.classList.remove("blur");
        });

        <% if (user && justLoggedIn) { %>
        var greetingDiv = document.getElementById("greetingDiv");

        // Add the 'blur' class to the main section when the greeting image appears
        mainSection.classList.add("blur");
        <% } %>
    });
</script>

<header>




    <nav>
        <div id="logo">
            <a href="/">MyGymBro</a>
        </div>

        <div id="nav-items">
            <ul>
                <li class="dropdown">
                    <a href="#">Services</a>
                    <div class="dropdown-content">
                        <a href="/inputFood">Calculate Calories</a>
                        <a href="/getUserCalories">Calories Chart</a>
                        <a href="/makeExercise">Make Workout Plans</a>
                        <a href="/workoutPlans">Workout Plans</a>

                        <a href="/goals">Update Goals</a>
                        <a href="/calculate">BMI</a>

                    </div>
                </li>
                <% if (!user ) { %> <!-- Show login/register links when user is not logged in -->
                <li><a href="/login">Login</a></li>
                <li><a href="/register">Register</a></li>

                <% } else { %> <!-- Show logout link when user is logged in -->

                <li><a href="/isloged">Profile</a></li>
                <li><a href="/logout">Logout</a></li>
                <% } %>
            </ul>
        </div>
    </nav>
</header>

<main>
    <% if (user && justLoggedIn) { %>
        <div id="greetingDiv">
            <h1>Hello, <%= user %>!</h1>
            <p>Your daily calorie goal is: <%= userGoalCal %></p>
            <p>Your daily exercise minutes goal is: <%= userGoalExercise %></p>
            <button id="closeGreetingButton">Close Message</button>
        </div>
    <% } else if (user) { %>

    <% }  %>

        <section id="hero">
            <h1>Welcome to <span class="shining-effect" data-text="MyGymBro">MyGymBro</span></h1>
            <p>Track your fitness progress and reach your goals.</p>
        </section>


</main>

</body>
</html>
